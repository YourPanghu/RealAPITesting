"""
==========================================================================
RealAPITesting - API 客户端封装层（类似 Page Object 模式）
==========================================================================
每个 API 提供方一个模块，封装请求逻辑。

设计思路（对标 Selenium Page Object）：
  - 测试用例只调用 API 类的方法（如 api.get_now_weather(...)）
  - 不直接写 requests.get()，URL/参数/错误处理都在 API 类里
  - 好处：接口变更只需改 API 类，测试用例不用动

三个 API 客户端：
  hitokoto_api.py —— 一言：随机句子，无需 Key，零门槛上手
  qweather_api.py —— 和风天气：实况/预报/空气质量，免费 50,000次/月
  juhe_api.py     —— 聚合数据：笑话/新闻，免费注册可用
"""
